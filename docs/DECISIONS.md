# Decision log

| #     | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Alternatives rejected                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------ | ---------------------------------------------------------------------------------------------------------------- |
| D-001 | LaTeX compiles in a sidecar TeX Live container, not in-process or in-browser                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | real `pdflatex`, so the V1 template compiles unchanged; Vercel serverless cannot host TeX Live                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | SwiftLaTeX WASM (bundled package set is fragile for exotic packages)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-002 | Drizzle + `postgres.js` from Next.js server code; browser never touches the DB                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | typed SQL, plain-SQL migrations, credentials stay server-side, portable off Supabase                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `supabase-js` from the browser (anon key is public, so the password gate would be cosmetic; needs full Supabase local stack)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-003 | PDFs stored as `bytea` in `resume_pdf`, behind `lib/storage.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ~100KB each, one dev/prod code path, snapshot is transactional with the resume                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Supabase Storage now (extra infra + divergent dev path)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-004 | The renderer emits fixed macro names (`\resumeSubheading`, `\resumeItem`, …)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | keeps templates in full control of layout without inventing a templating language                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | a general templating language (large scope); renderer emitting raw layout (template loses control)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-005 | Token syntax is `<<TOKEN>>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | an unsubstituted token renders as visible garbage — loud but non-fatal                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `\Macro` (hard TeX error), `%%X%%` (silently swallowed by the comment)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-006 | `technical_skill` gains a `technical_skill_row_id` FK not present in the spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | the bridge table has no row reference, so skills must know their row — same shape as `experience_bullet`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | keeping skills global and adding the row to the bridge PK (breaks the spec's stated composite PK)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-007 | Bridge-table presence means "selected"; `sort_order` is a dense 0-based sequence rewritten wholesale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | no `is_selected` column to drift out of sync; reorder is one idempotent transaction                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | nullable `sort_order` + `is_selected` flag                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-008 | Content is substituted into LaTeX **verbatim, unescaped**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | bullets legitimately contain `\textbf`, `\href`, `\$`; the user is authoring LaTeX                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | auto-escaping (would break every existing bullet)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-009 | Column is `sort_order`, not `order`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `order` is a reserved SQL keyword                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | quoting `"order"` everywhere                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-010 | Auth is one shared password → HMAC token in `localStorage`, verified by middleware                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | matches the spec's single-user gate; long password entered once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | cookies/sessions (more moving parts), real user accounts (out of scope)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-011 | Content is never deleted, only archived — no DELETE endpoints                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | resumes must keep rendering content that has since been retired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | soft-delete with cleanup job                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-012 | `GET /api/resumes/:id` returns the entire editor payload (resume + template + selections + library) in one response                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | the editor needs all of it immediately; avoids a request waterfall on load                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | separate endpoints per slice                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-013 | `technical_skill_row` carries a `separator` column                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | the real resume joins skill rows with `, ` but interest rows with ` $                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | $ `; a hardcoded separator cannot represent it, and representing the real resume is the project's acceptance bar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | hardcoding `, ` for top rows and ` $ | $ `for bottom rows (couples presentation to the`top` flag, and the user may want either style in either section) |
| D-014 | `lib/db/index.ts` exports lazy Proxies; the pool opens on first query, not at import                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `next build` evaluates every route module while collecting page data, so an eager `postgres(DATABASE_URL)` fails the build on any machine without a database — including CI                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | eager client (breaks builds), per-handler client construction (a new pool per request)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-015 | The latex sidecar requires a shared bearer token (`LATEX_SERVICE_TOKEN`) on `/compile`; `/health` stays open                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | a Vercel function cannot join Fly's private 6PN mesh, so the sidecar needs a public IP — and LaTeX can read files on the box it runs on, so an open `/compile` is not acceptable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Fly private networking (impossible cross-cloud), relying on hostname obscurity (the app name is in the repo), co-locating the app on Fly (changes the deployment target the spec asks for)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-016 | Templates support `<<IF:TOKEN>> ... <<ENDIF>>`, and every section in the default template is wrapped in one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | an empty section left `\begin{itemize}` with no `\item`, which is a **fatal** LaTeX error — deselecting a whole section (a normal tailoring action) broke the preview entirely. Dropping the block rather than just the token also removes the now-meaningless `\section{}` heading                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | emitting a placeholder `\item{}` (leaves an empty heading and a stray bullet); making the renderer strip list wrappers heuristically (requires parsing the template); leaving it to the template author (the original choice — it is a trap, and it fired)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-017 | Formatting and the project's layering invariants are enforced by Prettier + a small ESLint flat config, and the per-directory conventions live in `.claude/rules/*.md` scoped by `paths` globs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | the invariants that matter here are all silent — nothing fails at the moment you break them — so prose in a doc an agent may or may not have loaded is the wrong enforcement mechanism; a lint error at the point of the mistake, whose message names the rule file, is                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | leaving everything as prose (the status quo, which had already drifted in five places); a hand-rolled convention-checking script (non-standard, one more thing to maintain); putting every rule in the root `CLAUDE.md` (loads on every session regardless of relevance)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-018 | Multi-user: `user_id` on the five root tables only (`template`, `experience`, `project`, `technical_skill_row`, `resume`); children and bridge rows inherit their owner through their parent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | one source of truth per fact — a `user_id` on a bullet could contradict its experience's. Isolation on the inferred tables is enforced in the query layer (joins to the parent, and re-validating every content id in a selections body against the caller), proven by `lib/queries/isolation.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `user_id` on every table (duplicated, driftable state); composite `(id, user_id)` foreign keys to make it a schema-level invariant (forces `user_id` onto every child anyway, i.e. the same duplication with extra ceremony); Postgres RLS (the app connects as one role and would have to set a session variable per request — real isolation, but it moves the rule out of the code and into a place neither the tests nor the types can see)                                                                                                                                                                                                                                                                                                                                                        |
| D-019 | Three auth modes (`dev` / `password` / `supabase`) behind one `requireUserId()`, with dev mode auto-logging in as the first seeded user                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | local development must work with no credentials and no OAuth provider, while production must never silently lose its gate; every mode resolves to a `users` row, so the rest of the app is written once. Defaults are `dev` outside production and `password` in production, so shipping cannot turn the login screen off by omission                                                                                                                                                                                                                                                                                                                                                                                                                    | a single mode + a fake "local" OAuth provider (more moving parts than the thing it stands in for); deleting the password gate in favour of a Supabase stub (would have left no working deployable path at all); `NODE_ENV` alone with no override (no way to test password mode locally, which is what the e2e suite does)                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-020 | The password token gains a `sub` claim naming its user, and the token version bumps 1 → 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | a v1 token names nobody, so there is no correct way to scope its queries; invalidating them is the only honest answer. Cost: everyone is logged out once                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | defaulting unversioned tokens to the first user (a token minted before multi-user would silently become a key to whichever account sorted first)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-021 | Feedback screenshots are committed to an orphan `feedback-assets` branch via the Git Data API and linked at that commit's sha, rather than attached to the issue                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | GitHub's issue API has no attachment endpoint — the web UI's uploader is not public API. Linking at a sha makes the URL immutable, and the repo being public lets GitHub's image proxy render it inline. **If the repo ever goes private, inline rendering breaks** and the screenshot needs hosting by the app instead                                                                                                                                                                                                                                                                                                                                                                                                                                  | the Contents API (one call instead of five, but caps a file at 1 MB — well under a Retina screenshot); base64 in the issue body (bloats it and renders as a wall of text); not supporting screenshots (the single most useful part of a bug report)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-022 | _(Superseded by D-026: the board is gone; the rest stands.)_ Task state lives in GitHub issues on a Projects v2 board, not in `docs/`; `docs/` is restricted to present-tense descriptions of what exists, and the `Planning → Ready` column move is a **human approval gate** an agent may never make itself, with `Ready` and `In Progress` kept apart so "approved" and "claimed" are distinguishable                                                                                                                                                                                                                                                                         | `docs/WORKPLAN.md` was a board only one agent could see, `docs/STATE.md` had become a changelog describing the journey rather than the destination, and two agents editing either one collide in a way git cannot merge meaningfully. Issues are visible, concurrent, linkable from PRs, and already the destination of the in-app feedback widget (D-021), so the push and pull halves of the loop meet in one place. The human gate exists because an agent that plans and implements in one unbroken pass gives nobody a cheap moment to redirect it — the plan comment is the artifact to disagree with, and it costs a column move instead of a code review                                                                                         | keeping `WORKPLAN.md` as the board (invisible outside an editor, and the collision problem stands); a board with no planning column (loses the cheap redirect); collapsing `Ready` into `In Progress` (the board could then show that work was approved, or that an agent held it, but never which — so a queue of blessed unstarted work is invisible); making `Planning → Ready` automatic on the agent posting its plan (that is the status quo with extra ceremony — the gate only means something if a human has to move it); deleting `docs/` outright (the contracts and D-log are exactly the docs that earn their keep)                                                                                                                                                                       |
| D-023 | The feedback form is a non-modal popover anchored to its button, and the button itself is a screenshot dropzone                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | a bug report describes what is on screen, and a full-page modal with a dimmed backdrop hides the evidence; anchoring the form to its corner keeps the page readable and clickable, which also makes "click anywhere else" the natural dismissal. Dropping a screenshot on the button is the shortest path from ⌘⇧4 to a filed issue                                                                                                                                                                                                                                                                                                                                                                                                                      | keeping the modal (hides the thing being reported); a second floating drop target beside the button (another permanent element for an occasional gesture); ⌘V paste alone (already supported, but only reaches the clipboard, not a file on disk); letting the browser handle a missed drop (it navigates to the raw image and loses the page)                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-024 | The issue body's **Page** line is the route only; the origin and a readable browser name go in the Environment block                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | the route names a page in this codebase, so two reports about the same page read as the same page whether they came from prod or a laptop — `localhost:3000/resume/x` and `resumix.app/resume/x` were two different strings to skim and to group. Which deployment it was still matters, and that is what Environment is for                                                                                                                                                                                                                                                                                                                                                                                                                             | keeping the full URL on the Page line (buries the route inside a host that is nearly always the same); sending route and origin as separate form fields (a wire change for something the server derives with `new URL`); a UA-parsing dependency for the browser line (hundreds of regexes for crawlers and feature phones, for one caller); dropping the raw user-agent once parsed (nothing to fall back on when the parse is wrong)                                                                                                                                                                                                                                                                                                                                                                 |
| D-025 | Components take `ref` as an ordinary prop; `forwardRef` is not used                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | React 19 passes `ref` through as a normal prop, so `forwardRef` is legacy API here — and wrapping in it was the only reason `SearchBox` and `TemplateEditor` could not use the `export default function Name(props: NameProps)` shape every other component follows (.claude/rules/components.md). One shape for all of them, and one less import                                                                                                                                                                                                                                                                                                                                                                                                        | keeping `forwardRef` (works, but is the React 18 idiom and reads as though these two components are special); a `innerRef`-style custom prop (invents a second convention for something the platform now does)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-026 | **Supersedes D-022.** Task state is a `status:*` label on the issue, not a column on a Projects v2 board; `scripts/status.sh` is the only thing that sets it, and agents never file issues except an epic's sub-issues                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A user-owned Projects v2 board is reachable only from a classic PAT with `project` scope, which cannot be restricted to one repository — so "a credential that only touches this repo" and "a credential that can move the board" were mutually exclusive. It also failed silently: `board.yml` gated on a `BOARD_TOKEN` secret that was never set and no-oped rather than failing, so the sync was dead for the board's entire life without ever reddening a check. Labels need only `issues: write`, which CI's default `GITHUB_TOKEN` already has. Issues are the human's inbox, so an agent filing its own is a commitment nobody made                                                                                                               | Moving the repo and board into an organisation, which would have made a fine-grained PAT work — rejected as a large restructuring to keep a mechanism that is likely to be replaced by Linear or Jira, which labels migrate to and a bespoke board does not. Splitting into two tokens (repo-scoped + `project`-only) — rejected as two credentials to rotate for a board nobody was reading                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-027 | The task lifecycle is three hand-invoked skills in `.claude/skills/` — `triage`, `implement`, `review-pr` — that never invoke each other; `triage` sizes an issue into one of three tracks and writes a plan of proportional depth into the **issue body** between `<!-- resumix:plan -->` markers                                                                                                                                                                                                                                                                                                                                                                               | Three invocations means three places a human can stop the work, which is the only thing standing between "an agent planned it" and "an agent planned it, built it and merged it" — a chain that calls itself has one entry point and no brakes. Sizing exists because a one-line CSS fix and a schema change were getting identical ceremony, so either small work was over-planned or large work was under-reviewed; the small track self-approves because there was no architectural decision to approve. The plan lives in the body rather than a comment so a re-plan replaces it instead of burying it under the discussion                                                                                                                         | Subagents that chain automatically — rejected: it removes the human checkpoints that are the entire point. One skill with a mode flag — rejected: the three phases share almost no instructions, and one file would load all three into context for whichever was wanted. Inferring epics from size — rejected in favour of an explicit `epic` label, because guessing wrong creates issues nobody asked for                                                                                                                                                                                                                                                                                                                                                                                           |
| D-028 | The lifecycle has two paths: a human in a session takes approval in conversation and drives worktree → branch → PR directly, while the `.claude/skills/` skills exist for unattended runs. A request arriving mid-task is scope creep onto the current issue, and agents open an issue only when a human asks outright or the `epic` label is present                                                                                                                                                                                                                                                                                                                            | The ceremony that makes an unattended agent safe is pure friction when someone is watching: a human who has just said "go ahead" should never have to open GitHub and drag a label to say it again. The gate is not removed, only relocated — what stays forbidden either way is an agent deciding its own plan is approved. Defaulting a mid-task request to the current issue matches how people actually work; splitting on the agent's initiative produces issues nobody asked for and scatters one piece of work across several                                                                                                                                                                                                                     | One strict path for both — rejected: it makes the interactive case, which is the common one, tediously slow. Skills that detect whether a human is present — rejected: unreliable to detect, and failing open would silently remove the gate that protects the unattended case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-029 | **Refines D-028.** The attended and unattended paths are now physically separated rather than both described in `CLAUDE.md`. `CLAUDE.md` carries only the attended path — branch, build, gate, commit, approval in conversation, never file an issue — and the entire `status:*` lifecycle moved into `.claude/rules/github.md`, which loads only for `.github/**`, `scripts/status.sh`, `scripts/herd.sh` and `.claude/skills/**`. All three skills are `disable-model-invocation: true`                                                                                                                                                                                        | D-028 split the paths in prose, but prose in `CLAUDE.md` is loaded into _every_ session, so an ordinary request like "fix this warning" arrived pre-loaded with a lifecycle it was explicitly exempt from — and agents followed it anyway, filing issues and moving labels for work nobody tracked. Worse, the skills were model-invocable, so a task that merely resembled a description could trigger a whole phase unasked. Context is the enforcement mechanism: the ceremony cannot be skipped by an attended session if the attended session never reads it, and a skill cannot self-trigger if the model is not offered it. `scripts/herd.sh` is the only thing that starts them                                                                  | Keeping the lifecycle in `CLAUDE.md` with a louder "ignore this unless unattended" banner — rejected because D-028 already tried exactly that and the banner lost to the 130 lines under it. Deleting the unattended path entirely — rejected, it is what `herd.sh` drives. Putting the lifecycle in a fourth skill the other three call — rejected, skills that invoke skills is the phase-boundary collapse D-027 exists to prevent                                                                                                                                                                                                                                                                                                                                                                  |
| D-030 | Two halves of one rule. Every `orderBy` in the data layer (`lib/queries/**` and `lib/storage.ts`) ends in a unique tiebreaker column (`id`), so the result is always reproducible; and `scripts/seed.ts` stamps each content row with a monotonically increasing `created_at`, so the reproducible order is also the _authored_ one                                                                                                                                                                                                                                                                                                                                              | `ORDER BY created_at` over rows written in one transaction is a total tie — `created_at` defaults to `now()`, which is transaction-start time — so Postgres may return them in any order, and an `UPDATE` rewrites the heap tuple, which is precisely what makes it return a different one. Symptom: a bullet jumps position in the picker the moment you edit it. Found when three e2e tests added on #11 failed in CI and passed locally. The tiebreaker alone is not enough: it makes the order stable but arbitrary (id order), which silently reshuffled every seeded library away from the order `lib/seed-data/v1-resume.ts` writes them in — `archive.spec.ts` caught that. Determinism is the query's job; meaning is the seed's                | Stamping the seed alone (leaves every other transactional bulk insert just as non-deterministic). Adding an explicit `sort_order` to the content tables — that is what the _resume_ bridge tables are for, and library order is creation order, not a user-chosen one (D-007). Switching the column default to `clock_timestamp()` (a migration that narrows the tie without closing it, and changes what `created_at` means)                                                                                                                                                                                                                                                                                                                                                                          |
| D-031 | **Production is a second instance on the same laptop, not a cloud deployment.** It runs from its own clone (`~/.resumix/src`, always `origin/main`), its own Postgres volume, its own images and its own loopback-bound ports (39000/39432/39080), via one script, `npm run deploy:prod`. Development cannot reach it and agents may not run it — `scripts/deploy-prod.sh` aborts when `CLAUDECODE` is set and `.claude/settings.json` denies the script, the prod compose file and `~/.resumix/`.                                                                                                                                                                               | The problem was never hosting, it was that `npm run verify` runs `db:seed --force`, which `TRUNCATE`s every content table, and then lets Playwright edit whatever survives — so the database holding the real resume can never be the one the merge gate runs against. Separating the _instances_ solves that without a cloud account, a bill, or the LaTeX sidecar problem (D-001) that made Vercel awkward in the first place. The app is built into an image rather than run from a checkout because two Next processes sharing one `.next` corrupt it, and a production server that dies whenever someone runs `npm run build` is not one.                                                                                                           | Deploying to Vercel + Fly + Supabase (a real bill and three accounts for a single-user app; still needs a non-Vercel host for `pdflatex`). A hosted LaTeX API instead of the sidecar (free tiers are ~15 compiles/month against a live-preview pane that compiles per edit, and it ships raw resume content to a third party). Vercel + a tunnel to a sidecar on this laptop (same single-machine availability as running the whole app locally, but with three more moving parts). A separate _database_ rather than a separate instance (a `.env` away from being pointed at by the dev server, and leaves prod running unbuilt working-tree code).                                                                                                                                                  |
| D-032 | Job applications are **two** tables: `application` — a sixth root table carrying `user_id`, with a `status` enum (`draft`/`applied`/`interviewing`/`offered`/`rejected`) as the entire state model, plain-text `company`, one freeform `notes` column, and **two** nullable resume references — plus `application_file` for untyped attachments. `resume_id` is the live resume being tailored and moves on after sending; `resume_pdf_id` is the snapshot that was actually sent and never changes. `applied_at` is derived from `status` (`lib/applications/status.ts`), not settable by the API                                                                               | An application asks two different questions after the fact — "what did they receive?" and "which resume was that, and what has it become since?" — and neither reference can answer both. Keeping both is also why `resume_pdf_id` is `on delete restrict` while `resume_id` is `set null`: losing the editor link costs nothing, losing the evidence of what was sent costs the feature. Everything else is deliberately unstructured, because applications differ from each other enough that a schema for them would be filled in inconsistently and read by nobody. Extends D-018: the ownership rule is unchanged, there is simply a sixth root                                                                                                     | A `company` table (applying twice is two rows, and a picker plus a dedupe problem buys nothing at this size); an `application_event` log with `status` derived from the newest row (a real history, but every board read becomes a join and every status change a second write, for a timeline nobody asked to see); `application_task` and `contact` tables (structure imposed up front on the parts that vary most); pinning only the snapshot (loses "open the resume this came from"); keeping only the resume link (loses what was actually sent — the one thing a record of an application is for); a settable `applied_at` (a second source of truth for whether something was sent, free to contradict the status); a `kind` column on attachments (classification nobody would keep accurate) |
| D-033 | Applications are the front door. `/` is the applications board, the resume library moves to `/resumes`, and the board is split in two: a **Pipeline** kanban of the statuses that still need something (draft, interviewing, offered, rejected) and an **Applied** tab holding everything sent, as a searchable table. Logging an application clones a resume named after the company (`createResumeFrom`, defaulting to Default in the dialog), and `/resume/:id?application=<id>` is the same editor in application mode — back returns to the application, and the primary action saves the PDF to it via `POST /api/applications/:id/pdf` rather than downloading a snapshot | A resume is a means, not an end: the thing being tracked is the application, so it gets the home page. The split exists because the two halves have opposite shapes — the pipeline is a handful of live things you act on, while Applied is an ever-growing pile that is sent and forgotten, where density and search beat cards. An `applied` column would bury the four that matter. Cloning a resume per application removes the step nobody wants to do by hand, and makes "tailored for this company" the default rather than an act of discipline. Application mode is one bit of context that answers the only question the editor cannot: where does "done" go, and what does saving mean — a general library edit, or this application's resume | Keeping `/` as the resume grid with applications as a second tab (leaves the app pointed at the artifact rather than the work, and the grid grows a card per application anyway now that each one clones a resume); one board with all five columns and a collapsed Applied column (still one layout serving two very different reading tasks); creating the resume lazily on first edit (the application would sometimes have a resume and sometimes not, and every screen would have to handle both); a modal resume editor inside the application page (the editor is a two-pane, full-height document view — it does not fit, and it already exists as a route); inferring "came from an application" from the referrer or history (invisible in the URL, lost on reload, untestable)              |
| D-034 | The pipeline is a drag-and-drop kanban of the three live statuses (draft, interviewing, offered). `applied` and `rejected` are not columns: they are two half-width drop zones that appear under the board only while a card is being dragged, and both land the application in the Applied table. One `DndContext` for the whole board, `PointerSensor` only, the whole card as the handle, and no ordering is persisted within a column                                                                                                                                                                                                                                        | Both of those statuses are terminal in practice — one waiting, one over — so as columns they grow without bound and bury the few applications that are live. As drop zones they cost nothing until you reach for them, and dropping is a better gesture than a dropdown for a board. The card is the handle because there is nothing else on it to aim at, and a 4px activation distance keeps an ordinary click opening the application; nothing in our code suppresses the click that ends a drag, because dnd-kit's pointer sensor already swallows it with a document capture listener it removes 50ms later — a second guard of our own would eat the next genuine click instead (which is also why the e2e drag helper waits out that window)      | Keeping `applied`/`rejected` as ordinary columns (the pile problem this whole layout exists to avoid); showing the drop zones permanently (two dead rectangles under the board at all times, for a gesture used once per application); a drag handle on each card rather than the whole card (a smaller target for the only gesture the card has); `useSortable` per column to persist order (an application carries no `sort_order`, and inventing one would be a column of state nobody reads); status changes only through the picker on the detail page (correct but tedious — the board exists to make the common move one drag)                                                                                                                                                                  |
