# No trailing slash: in a worktree `node_modules` is a symlink to the main
# checkout (see CLAUDE.md), and `node_modules/` only matches real directories.
node_modules
.next/
out/
build/
dist/
*.tsbuildinfo
.env
.env*.local
# The real one lives at ~/.resumix/prod.env, outside the repo (D-031). This
# is belt-and-braces for a copy that lands here by accident; the committed
# template is `prod.env.example`.
prod.env
.DS_Store
coverage/
playwright-report/
test-results/
.pgdata/
lib/render/.scratch/
scripts/.scratch/
/test-results/
/playwright-report/
/blob-report/

# Claude Code: rules/ and settings.json are checked in (see .claude/README.md);
# per-machine overrides and runtime state are not.
.claude/settings.local.json
.claude/scheduled_tasks.lock
